## v1.3.8

### 新增
- 后台仪表盘（WordPress 风格概览页 `/admin/dashboard`）：KPI 卡片（总文章数 / 总留言数 / 累计浏览 PV / 本月发布）、近 30 天浏览趋势 SVG 图（柱=PV、折线=UV）、快捷操作按钮、最近文章与最近留言
- 侧边栏新增「概览」分组，仪表盘置顶；默认进入 `/admin` 即跳仪表盘

### 修复
- 仪表盘留言头像：加载失败或缺失时自动显示首字母色块兜底
- 后台 sticky 标题栏滚动时被裁剪/消失：移除导致问题的负边距 `-mx-6 -mt-8`，标题栏与侧边栏品牌区高度对齐（h-14）
- 后台内容区背景色始终显示（加载即有底色），右侧留白与左侧一致
- admin-logo 恢复并增加指向前台首页的超链接

### 变更
- selfhosted/lib/version.ts 版本号：1.3.7 → 1.3.8
- 构建改为 `next build --webpack`（更稳定）
