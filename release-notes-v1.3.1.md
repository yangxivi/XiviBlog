xiviblog selfhosted v1.3.1

## 纪念灰主题补全站灰度

- 「纪念灰」主题除素灰配色外，新增**整站灰度滤镜**（`filter: grayscale(1)`）：
  封面图、头像、页面背景等所有颜色一并去色，呈现真正的全站素灰
- 切换到其它任意主题时自动恢复原色，无需其它操作
- 明暗两态均生效；fixed 元素（顶栏、返回顶部）位置不受影响

## 升级方式

解压覆盖后重新构建即可（无依赖变更，不用重新 install）：

```bash
npm run build && npm start
```
